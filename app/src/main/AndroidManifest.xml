<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TosCompose"
        tools:targetApi="31">

        <activity
            android:name="xyz.edsync.apple_watch_menu.feature.main.AppleWatchMenuActivity"
            android:exported="false"
            android:label=""
            android:theme="@style/Theme.TosCompose" />

        <activity
            android:name="xyz.edsync.business_banking.feature.home.HomeActivity"
            android:exported="false"
            android:label=""
            android:theme="@style/Theme.BusinessBanking" />

        <activity
            android:name="xyz.edsync.business_banking.feature.splash.BusinessBankingSlashActivity"
            android:exported="false"
            android:label=""
            android:theme="@style/Theme.BusinessBanking" />

        <activity
            android:name="xyz.edsync.business_banking.feature.intro.BusinessBankingIntroActivity"
            android:exported="false"
            android:label=""
            android:theme="@style/Theme.BusinessBanking" />

        <activity
            android:name="xyz.edsync.business_banking.feature.login.BusinessBankingLoginActivity"
            android:exported="true"
            android:label=""
            android:theme="@style/Theme.BusinessBanking" />

        <activity
            android:name=".feature.main.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TosCompose">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />

        </activity>

    </application>

</manifest>